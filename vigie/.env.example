# Vigie — Variables d'environnement
# Copier en .env et remplir les valeurs réelles

# Clé secrète Flask (générer avec: python3 -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=CHANGE_ME_generate_random_key

# Mot de passe du compte admin Vigie
ADMIN_PASSWORD=CHANGE_ME_strong_password
